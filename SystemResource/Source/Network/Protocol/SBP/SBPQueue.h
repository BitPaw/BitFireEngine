#pragma once

#include "SBPData.h"

#include <File/File.h>

#define SBPNoData 0x01
#define DoDataClear 1

namespace BF
{
	class SBPQueue
	{
		/*
		private:
		File _file;
		size_t _dequeuedDataCursorPosition;

		public:

		SBPQueue();

		ByteStream& DataStream() { return _file; }

		unsigned char Enqueue(const SBPData& SBPData);
		bool Dequeue(SBPData& SBPData);

		bool Clear(const SBPData& sbpData);*/

		
	};
}